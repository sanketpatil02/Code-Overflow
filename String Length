#include <stdio.h>

int main()
{
    char name[100];
    int length = 0;
    printf("Please, Enter the String = ");
    scanf("%s",&name);
    
    for(int i=0;name[i]!='\0';i++) {
        length++;
    }
    
    printf("Length of String is %d",length);
}
